<nav class="bg-white shadow-sm">
  <div class="max-w-7xl mx-auto px-4 py-3 flex justify-between items-center">
    <%= link_to "GreenTrail", root_path, class: "text-xl font-bold text-gray-800 hover:text-green-600" %>

    <div class="flex items-center space-x-4">
      <% if traveler_signed_in? %>
        <p class="mr-4">Signed in as <%= current_traveler.full_name %></p>
          <%= link_to "Edit Profile", edit_traveler_registration_path, class: "text-blue-600 hover:underline" %>
          <%= link_to "Sign Out", destroy_traveler_session_path, method: :delete, class: "text-red-600 hover:underline ml-4" %>
      <% else %>
        <%= link_to "Sign Up", new_traveler_registration_path, class: "text-green-600 hover:underline" %>
        <%= link_to "Sign In", new_traveler_session_path, class: "text-blue-600 hover:underline ml-4" %>
<% end %>
    </div>
  </div>
</nav>