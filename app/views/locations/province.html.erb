<h2 class="text-2xl font-semibold mb-4"><%= @location.name %> – Towns</h2>
<ul class="space-y-2">
  <% @towns.each do |town| %>
    <li>
      <%= link_to town.name, location_path(town), class: "text-purple-600 hover:underline" %>
    </li>
  <% end %>
</ul>